# ── Never commit: personal / business context ────────────────
# Working notes for the next AI session. Contains company and
# client names, machine paths and business plans. Local only.
HANDOFF.md
_to_delete/

# ── Never commit: run artifacts ──────────────────────────────
# Fetched papers, scored lists and the dedup DB are regenerated
# on every run and can be large.
out/
screened/
seen.sqlite3
*.sqlite3

# ── Models ───────────────────────────────────────────────────
# GGUF weights are 18GB+. Use download_model.py instead.
*.gguf
models/

# ── Python ───────────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# ── Editors / OS ─────────────────────────────────────────────
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# ── Scratch ──────────────────────────────────────────────────
*.tmp
*.bak
